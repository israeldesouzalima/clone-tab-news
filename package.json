{
  "name": "clone-tab-news",
  "version": "1.0.0",
  "description": "Repositório dedicado a práticar e consolidar as aulas do curso.dev",
  "main": "index.js",
  "scripts": {
    "dev": "npm run services:up && next dev",
    "services:up": "docker compose --file infra/compose.yaml up -d",
    "services:stop": "docker compose --file infra/compose.yaml stop",
    "services:down": "docker compose --file infra/compose.yaml down",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "next": "^16.0.5",
    "pg": "^8.16.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "prettier": "^3.7.3"
  }
}
